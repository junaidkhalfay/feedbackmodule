<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.feedbackmodule">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Users.genRegistration"></activity>
        <activity android:name=".Users.profile" />
        <activity android:name=".Users.UserHome" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".questions.QuestionActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".questions.AnswersActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Users.splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Users.LoginActivity" />
    </application>

</manifest>